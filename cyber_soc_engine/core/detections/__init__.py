"""Detections domain package (Reorg R5 / #486).

Detection-rule sources and their management.

Callers import directly from ``core.detections.<module>``; the legacy
``services.*`` paths were removed with this move.
"""
