"""Auth domain package (Reorg R5 / #486).

Page-extension session tokens and connector origin trust.

Callers import directly from ``core.auth.<module>``; the legacy
``services.*`` paths were removed with this move.
"""
