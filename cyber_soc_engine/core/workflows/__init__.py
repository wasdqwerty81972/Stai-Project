"""Workflows domain package (Reorg R5 / #486).

Multi-agent workflow discovery, execution, run tracking, and
AI-assisted / custom workflow definitions.

Callers import directly from ``core.workflows.<module>``; the legacy
``services.*`` paths were removed with this move.
"""
