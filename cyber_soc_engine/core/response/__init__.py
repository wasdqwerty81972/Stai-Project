"""Response domain package (Reorg R5 / #486).

Autonomous containment actions and the approval workflow that
gates them.

Callers import directly from ``core.response.<module>``; the legacy
``services.*`` paths were removed with this move.
"""
